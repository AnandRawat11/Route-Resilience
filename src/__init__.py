"""
Route Resilience — src/

Top-level source package. Domains:
  core/         — Config, logging, I/O, exceptions, constants
  data/         — Phase 1: ingestion → tiling → splitting → augmentation → quality
  segmentation/ — Phase 2: deep learning road extraction models
  network/      — Phase 3: topology reconstruction + graph analytics
  visualization/ — Plotting and mapping utilities
"""
